# A simple Python script that defines a function to add two numbers and then calls that function with specific values.
def add_numbers(a, b):
    return a + b

result = add_numbers(5, 10)
print("The total is:", result)

